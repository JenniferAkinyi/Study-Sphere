{
  "name": "study-sphere",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2.5.4"
  },
  "packageManager": "npm@11.4.2",
  "dependencies": {
    "firebase": "^11.9.1"
  }
}
